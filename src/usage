local lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/wdesadwdaws/Constancy-UI/refs/heads/main/src/lua"))()

local Constancy = ConstancyUI:Init()

local tab = Constancy:CreateTab()

local button = tab:Button()


local label = tab:Label()

local info = tab:Info()

local warning = tab:Warning()
